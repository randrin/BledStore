import { parseRequestUrl } from "../utils";


const ProductScreen = {
    render: () => {
      const request = parseRequestUrl();
      const product = awai getPro
      return `<div>ProductScreen</div>`;
    }
}

export default ProductScreen;