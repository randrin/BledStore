export const getProduct = asyn (id) => {
    try {
        
    } catch (error) {
        
    }
}
    await axios({
    url: "http://localhost:5000/api/products",
    headers: {
      "Content-Type": "application/json",
    },
  });