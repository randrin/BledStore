const Checkout = {
    render: (props) => {
        return `
            <div class="checkout-steps">
                <div class="${}"></div>
            </div>
        `
    }
}

export default Checkout;